<!-- Auto-generated by `python3 -m scripts.adr.generate_index` — do not edit manually. -->

# ADR Index

| # | Status | Title | Scope | Summary | Revisit |
|---|--------|-------|-------|---------|---------|
| [001](001-co-locate-marketplace-and-plugin.md) | Accepted | Co-locate the marketplace and plugin in one repository | .claude-plugin/ | Keep the marketplace and plugin in one repository; the plugin version lives only in plugin.json, never duplicated in the marketplace entry. | A second plugin joins the marketplace, or stable-vs-latest channels are needed. |
| [002](002-generate-changelog-at-release.md) | Accepted | Generate the changelog at release time, not a running [Unreleased] | .agents/skills/releasing/, CHANGELOG.md, scripts/ci/release_notes.py | Generate each CHANGELOG entry from history at release time; do not maintain a hand-written [Unreleased] section. |  |
| [003](003-locate-skill-assets-relative-to-skill-directory.md) | Accepted | Locate skill assets relative to the skill directory | skills/ | Locate a skill's bundled files relative to the skill's own directory, not via a hook-written plugin root pointer. |  |
| [004](004-run-creative-commits-inline.md) | Accepted | Run creative-commits inline, not in a cheap-model subagent | skills/creative-commits/ | Run creative-commits inline rather than dispatching it to a cheap-model subagent; cost alone never earns a delegation — parallelism or independence must — and cost may then only choose its model. | A measurement, not an intuition, shows the delegation's saving is real, or commit-time context becomes a real constraint. |
| [005](005-mirror-declared-tooling-as-pr-checks.md) | Accepted | Mirror each skill's declared tooling as a pull-request check | .github/workflows/pr.yml, skills/ | Gate every pull request in CI with checks mirroring the tooling each skill declares, making CI the authoritative enforcement layer rather than local git hooks or release-time validation — hooks stay welcome as a faster-failing local aid, just no longer the layer enforcement depends on — so a skill that gains tooling gains matching checks in the same change, with branch protection requiring only the aggregate gate job. |  |
| [006](006-validate-the-declaration-to-catch-mirror-drift.md) | Accepted | Validate the declaration to catch mirror drift | .github/workflows/pr.yml, scripts/ci/validate_manifests.py | Catch mirror drift by having CI check each skill's declaration against the workflow — failing the build when a declared tool is never run — rather than making the declaration executable and retiring the mirror. | A skill ships a package.json rather than a pyproject.toml. |
| [007](007-keep-repo-scripts-stdlib-only.md) | Accepted | Keep repo scripts stdlib-only | scripts/ | Keep everything under scripts/ stdlib-only with no Python project at the repo root, and reach for a root project rather than per-script PEP 723 metadata if that ever changes. | A second script hand-parses a grammar this repo does not define, or the workflow substring-match already under the constraint causes a miss in practice. |
| [008](008-release-only-x-y-z-versions.md) | Accepted | Release only X.Y.Z versions | .agents/skills/releasing/, scripts/ci/versions.py | Release only X.Y.Z versions — no pre-release suffixes or build metadata — from one shared pattern, failing loudly at the pull-request gate rather than after the merge to main. | The plugin marketplace gains a pre-release or channel concept, or a release must reach a subset of users by some mechanism other than a second catalogue entry. |
| [009](009-keep-a-standing-develop-bypass.md) | Accepted | Keep a standing bypass on the develop ruleset |  | Keep the Admin bypass on the develop ruleset permanently, for release preparation and mid-release corrections only — ordinary work still goes by pull request — rather than removing it after rollout or routing preparation through release/X.Y.Z branches. | A second person gains write access, or a human stops needing to write to develop. |
| [010](010-pin-the-marketplace-entry-to-main.md) | Accepted | Pin the marketplace entry to main | .claude-plugin/marketplace.json | Pin the marketplace plugin entry to a github source at ref main — a branch, not a per-release tag — so what users install is the release branch rather than whichever branch happens to be the repository default, and assert the pin in CI. | The marketplace and plugin split repositories. |
| [011](011-make-scripts-a-package.md) | Accepted | Make scripts a package | scripts/ | Make scripts/ a Python package and run every script as `python3 -m scripts.<area>.<name>`, so the frontmatter parser can be imported once instead of adapted per directory; this amends ADR 007's plain-file invocation and retires its parser-duplication trigger, leaving its stdlib-only constraint intact; run the suite with `python3 -m unittest discover -s scripts -t .`. |  |
| [012](012-advertise-one-plugin-per-catalogue.md) | Accepted | Advertise one plugin per catalogue | .claude-plugin/, scripts/ci/validate_manifests.py | Advertise exactly one marketplace entry; a second fails the manifest check, so adding a plugin means an ADR plus a release flow that names both — not a generalised flow built up front. | A second plugin joins the marketplace. |
| [013](013-scope-adrs-by-the-paths-they-bind.md) | Accepted | Scope ADRs by the paths they bind | .github/workflows/pr.yml, .githooks/pre-commit, docs/adr/, scripts/adr/, skills/writing-adrs/ | Key ADR frontmatter to the paths a decision binds — exact paths and directory prefixes, never globs — rather than to free-text tags, and answer the reverse question, which decisions bind this file, from the pre-commit hook. | A third decision scopes `[]`, making decisions with no file home common enough that an empty scope stops carrying signal. |
| [014](014-cite-adrs-from-code-comments.md) | Accepted | Cite ADRs from code comments | .agents/, .githooks/, AGENTS.md, docs/, scripts/, skills/ | Cite ADRs from comments and docstrings as a documented convention — the number and what the decision forbids at that line, never the reasoning — fixing the citation format now, and deferring a check that citations survive until an archival defence first depends on one. | An ADR is archived on a defence that rests on a comment in code. |
| [016](016-anchor-every-default-path-to-the-module.md) | Accepted | Anchor every default path to the module | .agents/rules/testing.md, scripts/ | Anchor every script's default paths to `__file__`, read that anchor only on the `__main__` line, and require a `repo_root` everywhere below it — so tests inject a fixture root and never chdir, and a script given no path acts on the tree it ships in. | A script must resolve a path it was given no argument for against whichever checkout the caller is standing in, rather than against the one it ships inside. |
| [017](017-move-a-skills-deterministic-steps-into-shipped-code.md) | Accepted | Move a Skill's Deterministic Steps into Shipped Code | .agents/skills/, .github/workflows/pr.yml, skills/ | Move a skill's deterministic steps into a tool that ships beside it, needs no install step, and reports rather than edits, holding the skill's prose and that code together with a test that fails when they drift — not an external linter such as Vale. | A third skill ships tooling, so the per-skill coupling assertions want generalising, or nz-english's scope narrows back to prose only. |
| [018](018-admit-a-path-scoped-rule-as-an-archival-defence.md) | Accepted | Admit a path-scoped rule as an archival defence | docs/adr/, skills/writing-adrs/ | Admit a path-scoped rule as a third qualifying archival defence beside a code comment and a breach large enough to need its own ADR, qualifying only where its globs and any other named defence reach every path in scope bar the rule's own entry, judged by the author for now rather than checked by the generator or generated from frontmatter. | An ADR here is archived on a rule, making a coverage check specifiable; or the load semantics narrow — rules ceasing to load in a subagent, or shell-first reading becoming the default across sessions rather than one mode among several — degrading the archivals already made. |
| [019](019-do-not-generate-path-scoped-rules-from-adr-frontmatter.md) | Accepted | Do not generate path-scoped rules from ADR frontmatter | .agents/rules/, docs/adr/, scripts/adr/, skills/writing-adrs/ | Do not generate path-scoped rule files from ADR frontmatter at any granularity; a hand-written rule stating a constraint stays welcome, and INDEX.md with the `--for` lookup remains how a path reaches the decisions binding it. | A harness loads a path-scoped rule on the creation of a matching file or on a shell read, so what a rule reaches stops depending on how a session happens to read; or the mandated index read is measured and found not to be happening. |
